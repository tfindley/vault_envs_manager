[project]
name = "vem"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "hvac>=2.3.0",
]

[project.scripts]
vem = "vault_envs_manager.main:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
